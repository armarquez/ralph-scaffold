# .mise.toml — generated by ralph-scaffold
# Run `mise install` after cloning to get all tools at the right versions.
# Requires mise: curl https://mise.run | sh

[tools]
python = "3.12"          # EDIT: match your project's Python version
uv = "latest"
just = "latest"

[env]
UV_PYTHON = "python3.12" # EDIT: match [tools] python above
